<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard - Hackaton</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #f8f9fa; }
        .card-custom { box-shadow: 0 4px 6px rgba(0,0,0,0.1); border: none; }
    </style>
</head>
<body>

<div class="container py-5">
    <div class="row mb-4">
        <div class="col text-center">
            <h1 class="display-5 fw-bold text-primary">Panel de Administración</h1>
            <p class="lead">Selecciona acciones para tu sesión de trabajo actual.</p>
        </div>
    </div>

    <div class="row">
        <div class="col-md-5 mb-4">
            <div class="card card-custom p-4 bg-white">
                <h3 class="mb-3">Nueva Acción</h3>
                <form action="/agregar" method="POST">
                    <div class="mb-3">
                        <label class="form-label">Selecciona una opción:</label>
                        <select name="opcion" class="form-select form-select-lg mb-3">
                            <% opciones.forEach(function(op) { %>
                                <option value="<%= op.id %>"><%= op.nombre %> ($<%= op.costo %>)</option>
                            <% }); %>
                        </select>
                    </div>
                    <div class="d-grid">
                        <button type="submit" class="btn btn-primary btn-lg">➕ Agregar a Sesión</button>
                    </div>
                </form>
            </div>
        </div>

        <div class="col-md-7">
            <div class="card card-custom p-4 bg-white">
                <div class="d-flex justify-content-between align-items-center mb-3">
                    <h3>Resumen de Sesión</h3>
                    <form action="/limpiar" method="POST" style="display:inline;">
                        <button class="btn btn-outline-danger btn-sm">🗑️ Limpiar Todo</button>
                    </form>
                </div>

                <% if (seleccionadas.length === 0) { %>
                    <div class="alert alert-info">No has seleccionado ninguna acción todavía.</div>
                <% } else { %>
                    <ul class="list-group list-group-flush">
                        <% seleccionadas.forEach(function(item) { %>
                            <li class="list-group-item d-flex justify-content-between align-items-center">
                                <div>
                                    <strong><%= item.nombre %></strong>
                                    <br>
                                    <small class="text-muted">Hora: <%= item.timestamp %></small>
                                </div>
                                <span class="badge bg-success rounded-pill">$<%= item.costo %></span>
                            </li>
                        <% }); %>
                    </ul>
                    <div class="mt-3 text-end">
                        <h4>Total Acumulado: $<%= seleccionadas.reduce((acc, item) => acc + item.costo, 0) %></h4>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
</div>

</body>
</html>
