<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SoftKob - Gestión Hotelera</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --sk-blue: #1C4E80;
            --sk-yellow: #F4C21E;
            --bg-light: #F0F2F5;
        }
        body { font-family: 'Poppins', sans-serif; background-color: var(--bg-light); }
        
        .navbar-sk { background-color: var(--sk-blue); padding: 1rem; color: white; }
        .text-sk-blue { color: var(--sk-blue); }
        .bg-sk-blue { background-color: var(--sk-blue); color: white; }
        .text-sk-yellow { color: var(--sk-yellow); }
        
        .card-custom {
            border: none;
            border-radius: 15px;
            box-shadow: 0 5px 20px rgba(0,0,0,0.08);
            background: white;
            overflow: hidden;
        }
        
        .btn-primary-sk {
            background-color: var(--sk-blue);
            color: white;
            border: none;
            transition: 0.3s;
        }
        .btn-primary-sk:hover { background-color: #163d66; color: var(--sk-yellow); }
        
        /* Estilos para el Switch/Checkbox */
        .form-check-input:checked {
            background-color: var(--sk-blue);
            border-color: var(--sk-blue);
        }
    </style>
</head>
<body>

<nav class="navbar-sk d-flex justify-content-between align-items-center mb-4">
    <div>
        <h3 class="m-0 fw-bold">SoftKob <span class="text-sk-yellow" style="font-size:0.6em;">MOTEL EDITION</span></h3>
    </div>
    <% if (modo === 'dashboard') { %>
        <form action="/reset-total" method="POST" onsubmit="return confirm('¿Reiniciar TODA la configuración y datos?');">
            <button class="btn btn-outline-warning btn-sm">⚙️ Re-Configurar</button>
        </form>
    <% } %>
</nav>

<div class="container pb-5">

    <% if (modo === 'setup') { %>
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card card-custom p-5">
                    <h2 class="text-center mb-4 text-sk-blue">Configuración Inicial del Establecimiento</h2>
                    <p class="text-center text-muted mb-4">Antes de empezar, define las habitaciones de tu motel.</p>
                    
                    <form action="/guardar-config" method="POST">
                        <div class="mb-4">
                            <label class="fw-bold">Nombre del Motel</label>
                            <input type="text" name="nombreNegocio" class="form-control form-control-lg" placeholder="Ej: Motel Paraíso" required>
                        </div>

                        <h5 class="mb-3 border-bottom pb-2">Registro de Habitaciones</h5>
                        <div id="lista-habitaciones">
                            <% for(let i=1; i<=5; i++) { %>
                                <div class="row g-2 mb-2">
                                    <div class="col-4">
                                        <input type="text" name="room_names" class="form-control" placeholder="Nombre/Núm (Ej: 10<%= i %>)" required>
                                    </div>
                                    <div class="col-4">
                                        <select name="room_types" class="form-select">
                                            <option value="Sencilla">Sencilla</option>
                                            <option value="Jacuzzi">Con Jacuzzi</option>
                                            <option value="Suite">Suite Master</option>
                                        </select>
                                    </div>
                                    <div class="col-4">
                                        <div class="input-group">
                                            <span class="input-group-text">$</span>
                                            <input type="number" name="room_prices" class="form-control" placeholder="Precio" required>
                                        </div>
                                    </div>
                                </div>
                            <% } %>
                        </div>
                        <div class="text-center mt-2 mb-4">
                            <small class="text-muted">Por defecto mostramos 5 espacios, puedes editar esto en el código si necesitas más.</small>
                        </div>

                        <div class="d-grid">
                            <button type="submit" class="btn btn-primary-sk btn-lg">🚀 Guardar y Empezar</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    <% } %>


    <% if (modo === 'dashboard') { %>
        
        <div class="row">
            <div class="col-12 text-center mb-4">
                <h1 class="fw-bold text-sk-blue"><%= config.nombre %></h1>
                <p class="text-muted">Sistema de Control de Turnos y Habitaciones</p>
            </div>
        </div>

        <div class="row g-4">
            <div class="col-lg-5">
                <div class="card card-custom p-4 sticky-top" style="top: 20px; z-index: 1;">
                    <h4 class="mb-3 text-sk-blue"><i class="bi bi-clock-history"></i> Cierre de Turno</h4>
                    <form action="/agregar-turno" method="POST">
                        <div class="row mb-3">
                            <div class="col-6">
                                <label class="small fw-bold">Fecha</label>
                                <input type="date" name="fecha" class="form-control" required>
                            </div>
                            <div class="col-6">
                                <label class="small fw-bold">Trabajador</label>
                                <input type="text" name="trabajador" class="form-control" placeholder="Nombre" required>
                            </div>
                        </div>

                        <div class="alert alert-info py-2 small">
                            Marca las habitaciones que se <strong>alquilaron</strong> hoy.
                        </div>

                        <div class="table-responsive" style="max-height: 400px; overflow-y: auto;">
                            <table class="table table-sm align-middle">
                                <thead class="table-light sticky-top">
                                    <tr>
                                        <th>Habitación</th>
                                        <th class="text-center">¿Se usó?</th>
                                        <th>Extras ($)</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% config.habitaciones.forEach(function(hab) { %>
                                        <tr>
                                            <td>
                                                <strong><%= hab.nombre %></strong>
                                                <br>
                                                <small class="text-muted"><%= hab.tipo %> ($<%= hab.precio %>)</small>
                                            </td>
                                            <td class="text-center">
                                                <div class="form-check form-switch d-flex justify-content-center">
                                                    <input class="form-check-input" type="checkbox" name="uso_<%= hab.id %>">
                                                </div>
                                            </td>
                                            <td>
                                                <input type="number" name="extra_<%= hab.id %>" class="form-control form-control-sm" placeholder="0" min="0">
                                            </td>
                                        </tr>
                                    <% }); %>
                                </tbody>
                            </table>
                        </div>

                        <div class="d-grid mt-3">
                            <button type="submit" class="btn btn-primary-sk py-2">💾 Registrar Cierre de Turno</button>
                        </div>
                    </form>
                </div>
                
                <div class="card card-custom p-4 mt-4">
                    <h5 class="text-secondary">Consultar Período</h5>
                    <form action="/generar-informe" method="POST" class="d-flex gap-2">
                        <input type="date" name="fechaInicio" class="form-control" required>
                        <input type="date" name="fechaFin" class="form-control" required>
                        <button class="btn btn-outline-dark">🔍</button>
                    </form>
                </div>
            </div>

            <div class="col-lg-7">
                
                <% if (reporte) { %>
                    <div class="card card-custom bg-warning bg-opacity-10 border border-warning p-3 mb-3">
                        <div class="row text-center">
                            <div class="col-4">
                                <small>Turnos</small>
                                <div class="fw-bold fs-4"><%= reporte.dias %></div>
                            </div>
                            <div class="col-4">
                                <small>Extras</small>
                                <div class="fw-bold fs-4 text-danger">$<%= reporte.totalExtras %></div>
                            </div>
                            <div class="col-4">
                                <small>Total Caja</small>
                                <div class="fw-bold fs-4 text-success">$<%= reporte.totalCaja %></div>
                            </div>
                        </div>
                    </div>
                <% } %>

                <div class="card card-custom p-4">
                    <h5 class="mb-3">Historial de Turnos</h5>
                    <% if (turnos.length === 0) { %>
                        <p class="text-muted text-center py-4">No hay turnos registrados aún.</p>
                    <% } %>

                    <div class="accordion" id="accordionTurnos">
                        <% turnos.forEach(function(t, index) { %>
                            <div class="accordion-item border-0 mb-2 shadow-sm">
                                <h2 class="accordion-header">
                                    <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapse<%= index %>">
                                        <div class="d-flex w-100 justify-content-between me-3">
                                            <span>📅 <strong><%= t.fecha %></strong> - <%= t.trabajador %></span>
                                            <span class="text-success fw-bold">$<%= t.totalGeneral %></span>
                                        </div>
                                    </button>
                                </h2>
                                <div id="collapse<%= index %>" class="accordion-collapse collapse" data-bs-parent="#accordionTurnos">
                                    <div class="accordion-body bg-light">
                                        <table class="table table-sm table-bordered bg-white">
                                            <thead>
                                                <tr class="text-muted small">
                                                    <th>Habitación</th>
                                                    <th>Tarifa</th>
                                                    <th>Extras</th>
                                                    <th>Total</th>
                                                </tr>
                                            </thead>
                                            <tbody>
                                                <% t.detalles.forEach(function(d) { %>
                                                    <tr>
                                                        <td><%= d.nombre %> <small>(<%= d.tipo %>)</small></td>
                                                        <td>$<%= d.precioBase %></td>
                                                        <td class="<%= d.extra > 0 ? 'text-danger fw-bold' : '' %>">
                                                            $<%= d.extra %>
                                                        </td>
                                                        <td class="fw-bold">$<%= d.subtotal %></td>
                                                    </tr>
                                                <% }); %>
                                            </tbody>
                                        </table>
                                        <div class="text-end small text-muted">
                                            ID Turno: <%= t.id %>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        <% }); %>
                    </div>
                </div>
            </div>
        </div>
    <% } %>

</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>